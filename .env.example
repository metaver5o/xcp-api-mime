# Bitcoin Core RPC connection (public endpoint — no local node needed)
#
# Option 1: QuickNode (recommended, free tier available)
#   Sign up at https://www.quicknode.com/ and create a Bitcoin mainnet endpoint
#   BACKEND_CONNECT=your-endpoint.quiknode.pro
#   BACKEND_PORT=443
#   BACKEND_USER=<empty or as provided>
#   BACKEND_PASSWORD=<your token>
#   BACKEND_SSL=1
#   BACKEND_SSL_NO_VERIFY=0
#
# Option 2: GetBlock (free tier available)
#   Sign up at https://getblock.io/
#   BACKEND_CONNECT=btc.getblock.io
#   BACKEND_PORT=443
#   BACKEND_USER=<empty>
#   BACKEND_PASSWORD=<your API key>
#   BACKEND_SSL=1
#   BACKEND_SSL_NO_VERIFY=0
#
# Option 3: PublicNode.com (Free, no registration)
#   BACKEND_CONNECT=bitcoin-rpc.publicnode.com
#   BACKEND_PORT=443
#   BACKEND_USER=
#   BACKEND_PASSWORD=
#   BACKEND_SSL=1
#   BACKEND_SSL_NO_VERIFY=0
#
# Option 4: Your own remote Bitcoin Core node
#   BACKEND_CONNECT=your-server-ip
#   BACKEND_PORT=8332
#   BACKEND_USER=rpc
#   BACKEND_PASSWORD=your-rpc-password
#   BACKEND_SSL=0
#   BACKEND_SSL_NO_VERIFY=0

BACKEND_CONNECT=localhost
BACKEND_PORT=8332
BACKEND_USER=rpc
BACKEND_PASSWORD=rpc
BACKEND_SSL=0
BACKEND_SSL_NO_VERIFY=0

# Set to 1 to skip backend availability checks on startup
FORCE=0

# Bootstrap mode: "bootstrap" to download DB snapshot, or empty to skip
CATCH_UP=
