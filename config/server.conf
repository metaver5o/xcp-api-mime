[Default]
# API v2 (REST)
api-port = 4000
api-host = 0.0.0.0

# API v1 (JSON-RPC)
rpc-host = 0.0.0.0

# Mainnet
testnet = 0
regtest = 0

# Increase max request body size to 50MB
max-message-size = 50000000

# Required for UTXO lookups and compose
electrs-url = https://mempool.space/api
